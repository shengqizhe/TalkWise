# 更新日志（Changelog）

**知讲 TalkWise** — 大学讲座智能平台（Spring Boot 3 + Vue 3 + LangChain4j Agent）

> 格式约定：每次提交后在顶部追加一条，按「新特性 / 变更 / 修复」分类说明；
> 每条附提交短哈希，便于与 git 历史对照。

---

## 2026-09-11 · 评价分析重构：分批 Map-Reduce（解决评论多/上下文长）

**变更**
- `analyzeEvaluations` 重构为「预处理 → 分批 Map → Reduce（含两级）」链路，**上下文与评论总数解耦**，上千条评价可处理：
  - 预处理：内容去重合并 + 短评过滤（仅计数不分析）
  - 分批：**token 预算为主约束（一级参数）+ 条数安全阀**，运行时按内容量动态装批（bin packing）
  - Map：每批提炼**结构化限长产物**（问题/亮点/建议），单批失败重试一次后跳过并计数
  - Reduce：跨批合并同类、按提及次数排序；输入超预算自动触发**两级 Reduce**（递归，上限 3 层）
- **情感分移除**（产品决策）：产出改为讲座级分析报告（问题清单/正面反馈/改进建议/整体评估），不再逐条落库，工具由 WRITE 降为 READ
- 参数全部配置化（支持环境变量覆盖）：`agent.analysis.batch-max-tokens`（3000，一级参数）/ `batch-max-items`（40，安全阀）/ `min-content-length`（5）/ `reduce-input-budget-tokens`（8000）

**设计说明**（区分原理与假设）
- 原理性：上下文解耦、bin packing 装填、结构化 Map 产物（使 Reduce 可控）
- 假设性：上述数值为**标定值**，默认值待评测校准；换模型后应重新标定（token 估算器留有替换为模型 tokenizer 的接入点）

---

## 2026-09-11 · 评价情感分析选取策略改进（匿名盲评）

**变更**
- `analyzeEvaluations` 不再全量拼接评价，改为**选取评分两端各最多 10 条**（信息量最大的"最好 + 最差"评价）
- **双层去重**：按记录 id 去重（两端可能重叠）+ 按内容去重（忽略空白差异）
- **匿名盲评**：选取后打乱顺序，且 prompt 中**不提供学生原始评分、不暴露哪些来自高/低分端**——模型仅依据文字内容独立判断情感，避免分组标签与评分锚定偏差
- 返回文案说明选取口径（共 X 条 → 选取两端共 Y 条匿名分析），避免误解为全量分析

---

## 2026-09-11 · Tool Routing 对齐：候选集运行时校验 + 方法论文档

**修复**
- **候选集外工具调用运行时拒绝**：此前模型"幻觉"调用被路由裁剪掉的工具时，`dispatch` 仍能从全量注册表找到并执行（**绕过路由过滤**）；现在候选集在对话开始时固定并全程复用，`dispatch` 校验调用必须在候选集内，候选集外一律拒绝并记录日志

**文档**
- 新增 `docs/tool-routing.md`：分层 Tool Routing 方法论（工具卡片 → 确定性过滤 → 粗分类 → 检索 → 动态绑定精选）、与本项目实现的逐条对照、工具规模演进路径（15+/50+）、评测指标清单与兜底策略

---

## 2026-09-11 · 工具标签体系与调用前路由过滤

**新特性**
- **工具标签体系**：`@AgentTool` 新增 `domain`（域：lecture / registration / statistics / content / analysis / general）与 `roles`（角色限制）属性，7 个工具全部标注
- **调用前双层过滤（ToolRouter）**：
  1. 身份过滤——按当前用户角色裁剪（学生/游客上下文里不出现管理类工具）
  2. 意图过滤——按用户消息命中工具域，只下发相关域工具；lecture 基础域与 general 常驻；**未命中任何域则不裁剪**（安全兜底）
- **工具描述规范化**：每个工具按「做什么 / 什么时候用 / 什么时候不用（排他说明）/ 参数来源约束」四段式重写，降低相近工具混淆

**效果**
- 候选工具数下降（如教师问"分析评价"只下发相关 2-3 个工具），模型选择更准、token 更省
- 角色边界从"工具内校验"提前到"上下文不下发"，形成双重保障
- 路由日志可观测：`[Agent路由] 命中域 [...]，工具 7 → 3 个`

---

## 2026-09-11 · Agent 阶段 C：能力扩展

**新特性**
- **统计问答工具 `queryStatistics`**（教师/管理员）：自然语言映射预定义指标——总体概览（讲座数/报名数/本周新增/签到率）、分类热度排名、院系报名分布、报名排行；代码聚合数据、不生成 SQL；管理员统计全平台、教师仅自己的讲座
- **文案生成工具 `generatePromotion`**（教师）：单步 LLM 生成 150-250 字讲座宣传文案（亮点/适合人群/时间地点），替代模板拼接
- **评价情感分析工具 `analyzeEvaluations`**（教师，写操作）：逐条评价输出情感分（0-100）与改进建议，**落库 `evaluation` 空置字段** `sentiment_score` / `improvement_suggestion`，并汇总正面/中性/负面分布
- 角色判定助手 `AgentRoleHelper`：工具级数据范围控制（教师限本人讲座、学生禁用统计类工具）

**变更**
- 模型调用拆分为 `AgentLlmClient`（对话循环 + 单步文本生成），供功能点工具复用并避免循环依赖；`AgentEngine` 相应精简
- 悬浮球工具轨迹新增三个新工具的中文标签；README 功能清单同步更新
- 工程：`.mvn/wrapper` 移除后 `mvnw` 失效，构建改用全局 Maven（3.8.8 + JDK 17）

---

## 2026-09-11 · 敏感信息环境变量化（安全加固）

**安全**
- MySQL 密码 / 邮箱账号与授权码 / DeepSeek API Key 全部改为环境变量注入，`application.yml` 不再含任何明文凭据
- 启动前需设置：`MYSQL_PASSWORD`、`MAIL_USERNAME`、`MAIL_PASSWORD`、`OPENAI_API_KEY`（可用 `OPENAI_MODEL` / `OPENAI_BASE_URL` 覆盖模型与地址）

**文档**
- README 快速开始同步更新（环境变量设置说明与 Windows 示例）

---

## [未发布] Agent 阶段 D（计划中）

- 容量规划（统计 × 语义混合 + 教师确认）
- SSE 流式输出（逐字 + 工具状态实时展示）
- 会话/消息/trace 落库（替代内存会话）

---

## 2026-09-08 · 新增 README 项目文档

**文档**
- 新增 `README.md`：项目简介、三端与 Agent 功能清单、技术栈、架构概览、快速开始（含 JDK 17 环境提示）、项目结构、分支与提交约定
- 确立更新约定：**功能或用法有变化时同步更新 README**（与 CHANGELOG 同步维护）

---

## 2026-09-08 · 会话并发加固

**加固**
- 同一用户对话请求**串行化**：`ReentrantLock`（每用户一把锁，30 秒等待上限），防止同一账号并发请求交错污染会话历史；超时返回"我还在处理上一条消息"提示
- 会话隔离边界明确：按 userId 隔离存储 + 请求级 ThreadLocal 身份（AgentContext）+ 每用户锁，三层保障学生/教师并发使用互不干扰

**文档**
- `docs/agent-改造方案.md`：阶段表更新为最新版（A/B/加固 已完成标记、C-F 规划）；简历表述更新（写操作直接执行、DeepSeek/LangChain4j）；约束清单修正重复条目

---

## 2026-09-08 · Agent 阶段 B：前端接入

**新特性**
- 悬浮球（FloatingAiBot）切换至 `/api/agent/chat`：纯对话式交互，报名/取消在对话中直接完成
- **工具调用轨迹展示**：回复下方以标签显示「🔧 查询讲座 / 报名讲座 / 取消报名」，直观呈现"AI 做了什么"
- 管理员控制台（AdminLayout）挂载 AI 助手入口（管理员可用自然语言查数据）
- 后端 `AgentChatResponse` 新增 `tools` 字段（工具名 + 参数摘要）；`AgentEngine` 返回结构化结果 `ChatResult{reply, tools}`

**变更**
- 前端新增 `api/agent.js`（超时放宽至 60s，适配多轮工具调用）；
- `StudentAi.vue` 同步切换新接口
- 旧 `/api/ai/*` 接口保留并行（验证通过后下线）

---

## 2026-09-08 · 写操作直接执行（`d056af3`）

**变更**
- `AgentEngine`：WRITE 工具从"拦截占位"改为**直接执行**；执行前记录审计日志（用户、工具、参数）
- 系统提示词更新：用户明确指令直接执行并如实回告、意图不明确先询问、禁止编造执行结果
- 登记为写操作安全底线：身份只取当前登录用户（防越权）、Service 层保底校验（防重复/容量/时间）、全程审计留痕

**新特性**
- 新增写工具 `registerLecture`（对话式报名）、`cancelRegistration`（对话式取消报名）
- 查询工具输出补充讲座 ID（写工具定位讲座所必需）

**文档**
- `docs/agent-改造方案.md` 同步 8 处：确认审批流 → 写操作直接执行（§5.3 及功能版图、场景轨迹、数据表、接口、前端、阶段表、约束清单）

---

## 2026-09-08 · 模型切换 DeepSeek（`d7d8b25`）

**变更**
- LLM 从通义千问（qwen-turbo @ DashScope）切换为 **DeepSeek（deepseek-v4-flash @ api.deepseek.com）**，OpenAI 兼容协议
- 配置支持 `OPENAI_API_KEY` / `OPENAI_MODEL` / `OPENAI_BASE_URL` 环境变量覆盖；Agent 引擎与旧 `/api/ai` 接口共用同一配置

---

## 2026-09-08 · Agent 阶段 A：最小闭环（`6f80c75`）

**新特性**
- 新增 `com.example.lecture.agent` 模块（11 个文件）：
  - `@AgentTool` / `@AgentParam` 注解 + `AgentToolRegistry`：启动扫描工具方法，模型参数 JSON 自动注入并反射执行——新增工具 = 加一个注解方法
  - `AgentEngine`：多轮对话循环（模型→工具执行→结果回填→再问模型，最多 8 轮），内存会话窗口（最近 20 条），工具结果超长自动截断
  - `AgentContext`：登录用户身份 ThreadLocal 透传，工具不接受模型传入的 userId（防越权）
  - `AgentProperties`：`agent.*` 配置映射
- 两个只读工具：`searchLectures`（查讲座）、`getMyRegistrations`（查我的报名）
- 新接口 `POST /api/agent/chat`：Sa-Token 取登录用户；游客可咨询讲座类问题

**变更**
- `pom.xml`：引入 LangChain4j 0.35.0（core + open-ai，不引 spring starter 以避免与 Spring Boot 3.1.5 耦合）
- `application.yml`：新增 `agent.*` 配置段
- `AiAssistantServiceImpl`：API key 从源码硬编码迁移至配置——**源码不再包含任何密钥**

---

## 2026-09-08 · 初始提交：UI 重构与品牌命名（`11cbb2a`）

**新特性**
- 全站黑白极简 UI 重构（对齐设计稿）：
  - 学生端：首页（Hero + 插图）、讲座列表（筛选 + 卡片网格）、我的预约、个人中心、讲座详情、AI 页
  - 教师端：工作台（统计 + 近期讲座）、我的讲座（表格管理 + 创建/编辑对话框）、预约名单（签到管理）、历史记录、个人中心、评价反馈
  - 管理员端：控制台布局（顶部导航 + 左侧栏）、仪表盘、审核队列、发布管理、分类管理、数据报表（CSV 导出）、权限设置、系统通知
  - 公共页：首页（Hero）、登录、注册、找回密码
- 全局主题：Element Plus 主色改黑 + 统一卡片/表格/按钮样式（`styles/theme.css`）
- 品牌命名：**知讲 TalkWise**（中英文名，登录页/顶栏/文档全站同步）
- 删除地图导航功能（页面、路由、入口）

**修复**
- `RoleManagement` 的 import 路径错误（`../api` → `../../api`，此前无法编译）
- 修改密码接口传参错误（改为传表单对象）

**文档**
- `docs/agent-改造方案.md`：Agent 化改造定稿（工具注册/对话循环/容量规划/数据设计/实施阶段）

**工程**
- `.gitignore` 补充前端 `node_modules` / `dist`
- 求职简历 PDF 已从全部 git 历史中移除（`git filter-branch` 重写 + 强推）
